# Environment Configuration
# SECURITY WARNING: Never commit this file with real secrets to git!
# Copy this file to .env and update with your actual values

# API Configuration
PROJECT_NAME=DevSecOps Social App
VERSION=0.1.0
API_V1_STR=/api/v1

# Security - CHANGE THESE IN PRODUCTION!
SECRET_KEY=your-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS - Update with your frontend URLs
ALLOWED_ORIGINS=["http://localhost:3000"]

# Database
DATABASE_URL=postgresql+asyncpg://user:password@db/app_db
# The default DATABASE_URL points to the local PostgreSQL instance running in the dev container.
#
# To use a Neon database (or any other external PostgreSQL), replace the value above
# with your Neon connection string. Example:
# DATABASE_URL="postgresql+asyncpg://user:password@ep-cold-mud-12345.ap-southeast-1.aws.neon.tech/dbname?sslmode=require"

# Environment
ENVIRONMENT=development
DEBUG=true
