# Production Dependencies

# Framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Data Validation & Settings
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Authentication & Security (Modernized)
# Replaces python-jose
PyJWT>=2.10.0
# Replaces passlib and bcrypt
pwdlib>=0.2.0
argon2-cffi>=23.1.0

# Critical Security Fix (DoS protection)
python-multipart>=0.0.20

# Database
sqlalchemy>=2.0.36
alembic>=1.14.0

# Database Driver
# Use this for production (no binary wheels)
# psycopg[binary]>=3.2.0

# Utilities
email-validator>=2.2.0
python-dotenv>=1.0.1
