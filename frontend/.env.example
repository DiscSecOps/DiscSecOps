# Frontend Environment Variables
# Copy this file to .env and update values for your environment
# ⚠️ NEVER commit .env to git - only commit .env.example

# =============================================================================
# API Connection
# =============================================================================
# Base URL of your backend API (must include /api/v1 to match backend versioning)
# This is read by frontend/src/config/index.js
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Legacy: VITE_API_URL is used by some services as a direct fallback
VITE_API_URL=http://localhost:8000/api/v1

# API Request timeout in milliseconds
VITE_API_TIMEOUT=10000

# =============================================================================
# Environment
# =============================================================================
# Current environment: development, staging, production
VITE_ENV=development

# =============================================================================
# Feature Flags (Optional)
# =============================================================================
# Example: Toggle features on/off
VITE_ENABLE_SOCIAL_FEATURES=true

# Enable debug mode (console logs, error details)
VITE_ENABLE_DEBUG=true

# Enable analytics (Google Analytics, etc.)
VITE_ENABLE_ANALYTICS=false

# =============================================================================
# Optional: External Services
# =============================================================================
# Uncomment and configure if you add these services:

# Google Analytics
# VITE_GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# Sentry Error Tracking
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# Feature Toggles
# VITE_FEATURE_USER_DASHBOARD=true
# VITE_FEATURE_ADMIN_PANEL=false

# =============================================================================
# Notes for Developers
# =============================================================================
# 1. All Vite environment variables MUST start with VITE_
# 2. Variables are embedded at build time (not runtime)
# 3. Never put secrets here - all VITE_ vars are exposed to the client
# 4. Access variables using: import.meta.env.VITE_YOUR_VAR
# 5. After changing .env, restart the dev server (npm run dev)
